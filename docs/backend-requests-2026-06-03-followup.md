# Follow-up по specialist-payslip — 2026-06-03

Продолжение к `docs/backend-requests-2026-06-03.md`. Перепроверено на живых данных
(суперадмин, только GET; подопытный — Мирстан Жакыпбекович,
`employee=af4ac413-d375-4b5e-bbb7-1a8506f27419`, филиал Главный
`branch=e0789b5e-8cbd-403d-ad85-0580c669f4f5`, `month=2026-05&periodHalf=first`).

---

## ✅ Q4 — закрыт (commit `f0695d7`). Спасибо.

`slot.earned` теперь включает фикс-ставку за приём. Проверено: у Мирстана
`earned=500` в каждом слоте. Частичная оплата учитывается — 15.05 один приём оплачен
частично, `earned=300` вместо 500. Фронт уже рендерит `earned`/`sumEarned`, правок не
требуется.

## ✅ Q2 — тоже закрыт (хоть в ответе и не упомянут). Подтверждаем.

Детализация `days[].slots[]` / `days[].totals` теперь **фильтруется по специалисту**.
Проверка на 01.05.2026:

| Метрика | Было (баг) | Сейчас | Факт |
|---|---|---|---|
| `totals.count` | 90 | **7** | в филиале 90 приёмов, Мирстан исполнитель в **7** |
| `totals.sumTotal` | 255 600 | **7 000** | 7×1000 |

Кросс-чек `GET /appointments/?date=2026-05-01&branch=...`: 90 приёмов в филиале, из них
Мирстан исполнитель ровно в 7 → лист отдаёт ровно его приёмы. Баग ушёл.

## ✅ Q3 — по поведению закрыт (просьба подтвердить на словах)

Сейчас `slot.sum` = цена строки услуги специалиста (1000), а не `total` всего приёма;
счётчик — по его строкам. Это то, что и просили. Если в приёме несколько исполнителей —
подтвердите, что частичная оплата делится между строками по той же логике
`payment_factor` (тест-кейса с двумя исполнителями в одном приёме у нас под рукой нет).

---

## ✅ Q1 — month-scope работает, ничего делать не надо

Запрос без `periodHalf` отдаёт весь месяц: `period.detailScope: "month"`, 31 день
(01.05–31.05), month total count=159 / sumEarned=77800, summary.gross=78000
(расхождение 200 = частичная оплата + off-grid, ожидаемо). Фронт уже доработан —
в диалоге добавлен переключатель «Весь месяц» (`SpecialistPayslipDialog`), PDF-рендер
месячный summary уже умеет.

---

## Осталось открытым

- **Q5 [СРЕДНИЙ]** — `branch.logoUrl` (+ способ загрузки) для логотипа на печатных формах
  по филиалам. Детали — в основном файле `docs/backend-requests-2026-06-03.md`.

Спасибо — весь блок по расчётному листу (Q1–Q4) закрыт. Остался только Q5 (логотипы).
