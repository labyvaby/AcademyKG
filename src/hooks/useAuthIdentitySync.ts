/**
 * useAuthIdentitySync — отключён (был привязан к Supabase).
 * Оставлен как заглушка для совместимости импортов в App.tsx.
 */
export function useAuthIdentitySync() {
  // no-op: identity sync not needed with Academy KG REST API
}
